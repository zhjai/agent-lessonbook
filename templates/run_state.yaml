# run_state.yaml — task checkpoint (WORKING MEMORY, not a source of truth)
# Records what the agent believes it did + the constraints in force. Not authority.
# One per task: state/tasks/<task_id>/run_state.yaml

task_id: ""
title: ""
status: in_progress            # free-form task status for YOUR workflow

# Pulled from control/ at resume (resume-context skill), plus corrections captured this task:
active_constraints: []         # rules + relevant approved lessons + mid-task clarifications in force now
                               # e.g. "chart titles must include the run name"

# Pointers to the worker-writable journals for this task:
correction_journal: "correction_journal.md"   # clarified requirements + drift notes, as they happen
candidate_lessons: "candidate_lessons.md"      # proposed lessons, PENDING human review

open_questions: []             # unmet preconditions / things to confirm — don't silently proceed past these

updated_by: "worker-agent"     # provenance; worker writes this file (it is belief, not verified truth)
